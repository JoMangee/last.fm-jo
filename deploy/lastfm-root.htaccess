Options -Indexes
DirectoryIndex index.php

# LiteSpeed/Apache: stop 301-redirecting directory requests to trailing slash.
# Without this, POST /scrobble gets redirected to POST /scrobble/ which
# LiteSpeed converts to a GET, causing a 405 from our PHP endpoint.
DirectorySlash Off
